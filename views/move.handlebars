<h2>All of your moves</h2>
<div class="all-playbooks">
    {{#each moves as |move|}}
    <div class="playbook-container">
        <h3>{{move.name}}</h3>
        {{{move.description}}}
        <a href="/origin" data-name="{{move.name}}" class="playbook"><button>Select</button></a>
    </div>
    {{/each}}
</div>