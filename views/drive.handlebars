<h2>Choose one of the drives below</h2>
<h3 class="center">At the end of a session, if you’ve met your drive’s
requirement, mark XP.</h3>
<div class="all-playbooks">
    {{#each drives as |drive|}}
    <div class="playbook-container">
        <h3>{{drive.name}}</h3>
        <p>{{drive.description}}</p>
        <a href="/origin/{{drive.playbook}}" data-name="{{drive.name}}" class="playbook"><button>Select</button></a>
    </div>
    {{/each}}
</div>